using System;

namespace fool
{
    class Program
    {
        static void Main(string[] args)
        {
            Game newgame = new Game();
            // while both players hands have cards in it, the game is still going
                // if game.turn == true
                    // run player1's turn
                // else:
                    // run player2's turn
        }
    }
}
